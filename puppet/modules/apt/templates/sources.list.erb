# <%= puppet_header %>
# <%=  __FILE__.gsub(/.*?puppet\//,fqdn + ':') %>
<%
repos.keys.sort.each do |repo| %>
# <%= repo %>
#   <%= repos[repo]['comment'] %>
#
<%  repo_types.sort.each do |repo_type|
        if repos[repo][repo_type] then
            repos[repo][repo_type].sort.each do |url|
-%>
<%= repo_type %> <%= url %>
            <%- end -%>
        <%- end -%>
    <%- end -%>
<% end %>
