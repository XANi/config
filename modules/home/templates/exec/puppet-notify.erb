#!/bin/bash
# Puppet: <%=  __FILE__.gsub(/.*?puppet\//,fqdn + ':') %>
# hack around root
source /home/xani/env.xsession
sudo -u xani /usr/local/bin/notify-wrapper puppet "$1" /usr/share/icons/oxygen/128x128/categories/applications-utilities.png
